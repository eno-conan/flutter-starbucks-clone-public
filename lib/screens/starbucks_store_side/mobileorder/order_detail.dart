import 'package:flutter/material.dart';

///オーダー詳細画面
class StoreSideOrderDetail extends StatelessWidget {
  const StoreSideOrderDetail({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
